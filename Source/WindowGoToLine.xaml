<Window x:Class="LogViewer2.WindowGoToLine"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LogViewer2"
        mc:Ignorable="d"
        Title="Go To Line" ResizeMode="NoResize" Height="100" Width="700" Icon="Icons/Line.png" WindowStartupLocation="CenterOwner"  MinHeight="100" MinWidth="300">
    
    <Grid Margin="10,10,10,10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="22" />
        </Grid.RowDefinitions>

        <TextBox x:Name="textLine" FontFamily="Consolas" Margin="0,0,0,5" Text="" IsTabStop="True" TabIndex="0"/>

        <WrapPanel Grid.Row="2" HorizontalAlignment="Right" Margin="0,0,0,0">
            <Button IsDefault="True" Name="btnOk" MinWidth="60" Margin="0,0,10,0" Click="BtnOk_Click">_OK</Button>
            <Button IsCancel="True" Name="btnCancel" MinWidth="60" Margin="0,0,0,0">_Cancel</Button>
        </WrapPanel>

    </Grid>
    </Window>
